[project]
name = "docker-project-1"
version = "0.1.0"
description = "Dev to Prod with FastAPI"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "hiredis>=3.2.1",
    "redis>=6.4.0",
]
